transformer_data_path: "data/shakespeare.txt"
rlhf_data_path: "data/rlhf.jsonl"
seq_len: 96
epsilon: 0.2
transformer_epochs: 3  # smoke test; increase to 3-10 for better base model
rlhf_epochs: 5      # smoke test; increase to 5-20 depending on labels
batch_size: 128        # reward model supervised batches
num_seq: 128                    # number of completions per rollout batch
epochs_per_rollout: 4          # PPO epochs over the rollout data
minibatches_per_rollout: 8     # PPO minibatches per epoch
total_steps: 28500